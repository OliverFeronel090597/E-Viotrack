# E-Viotrack
E-Viotrack is a python base applicarion function to connect to a long range RFID reader and query RFID user in database (sqlite/.db) display if dricver/user has active unpaid violation or not
